pipeline
{
agent
  {
  label "windows_slave"
  }
  tools {
  jdk "jdk1.8"
  maven "MAVEN_HOME"
  }
 stages {
   stage('build')
   {
     steps {
        bat 'mvn clean install'
      }
    }
}
}
